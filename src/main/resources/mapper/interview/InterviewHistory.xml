<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.interviewgo.mapper.interview.InterviewHistoryMapper">
	<!-- ID 기반 단일 개체 반환 -->
	<select id="selectLastInterviewHistoryBySsid">
		SELECT * FROM interview_history WHERE iv_ssid = #{iv_ssid} AND mb_uid = #{mb_uid} ORDER BY iv_step DESC LIMIT 1
	</select>
	
	<!-- ID 기반 단일 세션의 전체 개체 반환 -->
	<select id="selectAllInterviewHistoryBySsid">
		SELECT * FROM interview_history WHERE iv_ssid = #{iv_ssid} AND mb_uid = #{mb_uid}
	</select>
	
	<!-- 단일 개체 삽입 -->
	<insert id="insertInterviewHistory" parameterType="interviewHistory">
		INSERT INTO interview_history(
			mb_uid,
			iv_ssid,
			iv_step,
			iv_context,
			iv_score,
			iv_feedback
		)
		VALUES(
			#{mb_uid},
			#{iv_ssid},
			#{iv_step},
			#{iv_context},
			#{iv_score},
			#{iv_feedback}
		)
	</insert>
	
	<!-- ssid 기반 면접기록 삭제 -->
	<delete id="deleteInterviewHistoryBySsid">
		DELETE FROM interview_history WHERE iv_ssid = #{iv_ssid} AND mb_uid = #{mb_uid}
	</delete>
	
	<delete id="deleteInterviewHistoryByMbid" parameterType="Long">
		DELETE FROM interview_history
		WHERE mb_uid = #{mb_uid}
	</delete>
	
	<!-- 면접 기록 가져오기. (시간, step 순서) -->
	<select id="selectInterviewHistory" parameterType="Long" resultType="interviewHistory">
        SELECT * FROM interview_history 
		WHERE mb_uid = #{mb_uid} 
		ORDER BY
		    MAX(iv_date) OVER(PARTITION BY iv_ssid) DESC, 
		    iv_step ASC
    </select>
</mapper>