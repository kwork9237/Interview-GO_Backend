<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.interviewgo.mapper.interview.InterviewHistoryMapper">
	<!-- 인터뷰 기록 테이블 -->

	<select id="getInterviewHistoryLimit">
		SELECT * FROM interview_history WHERE mb_uid = #{mb_uid} LIMIT #{limit}
	</select>
	
	<select id="getInterviewHistortByID">
		<!-- 멤버 UID는 일시적으로 제거함 (2025 12 31) -->
		SELECT * FROM interview_history WHERE iv_ssid = #{iv_ssid} ORDER BY iv_step DESC LIMIT 1
	</select>
	
	
	<insert id="insertInterviewHistory" parameterType="interviewHistory">
		INSERT INTO interview_history(
			mb_uid,
			iv_ssid,
			iv_step,
			iv_context,
			iv_score,
			iv_feedback
		)
		VALUES(
			#{mb_uid},
			#{iv_ssid},
			#{iv_step},
			#{iv_context},
			#{iv_score},
			#{iv_feedback}
		)
	</insert>
	
	<delete id="deleteInterviewHistory" parameterType="String">
		DELETE interview_history WHERE iv_ssid = #{iv_ssid}
	</delete>
</mapper>